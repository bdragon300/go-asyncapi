{{- /*
    .Object == render.Server;
    .Config == common.ConfigDiagram
    */}}
{{define "diagram/server"}}
{{- if $.Config.ShowDocumentBorders}}{{goID $.Object.Pointer.Location}}.{{end}}{{ goID $.Object }}: "🖥 [{{$.Object.Protocol}}] {{$.Object.Host | toQuotable}}" {
    class: server
    {{- with tryTmpl (print "diagram/server/proto/" $.Object.Protocol) $ }}{{.}}{{end}}
}
{{- end}}
