{{- /*
    .Object == render.Channel;
    .Config == common.ConfigDiagram
    */}}
{{define "diagram/channel"}}
{{- if $.Config.ShowServers }}
    {{- range $server := $.Object.BoundServers }}
        {{if $.Config.ShowDocumentBorders}}{{goID $server.Pointer.Location}}.{{end}}{{goID $server}}.{{ goID $.Object }}: {{with $.Object.Address}}"{{$.Object.Address | toQuotable | ellipsis 25}}"{{end}} {
            class: channel
        }
    {{- end}}
{{- else }}
    {{- if $.Config.ShowDocumentBorders}}{{goID $.Object.Pointer.Location}}.{{end}}{{ goID $.Object }}: {{with $.Object.Address}}"{{$.Object.Address | toQuotable | ellipsis 25}}"{{end}} {
        class: channel
    }
{{- end}}

{{- end}}