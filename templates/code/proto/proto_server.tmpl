{{- /* dot == render.Server */}}

{{define "code/proto/server/newFunction"}}
func New{{ . | goIDUpper }}(producer {{goPkgUtil .Protocol}}Producer, consumer {{goPkgUtil .Protocol}}Consumer) *{{. | goIDUpper}} {
    return &{{. | goIDUpper}}{
        producer: producer,
        consumer: consumer,
    }
}
{{- end}}

{{define "code/proto/server/securityInterface"}}
type {{. | goIDUpper}}Security interface {
    {{goPkgRun}}AnySecurityScheme
    {{. | goIDUpper }}Security()
}
{{- end}}

{{define "code/proto/server/connectFunctions"}}
{{- with impl .Protocol}}
type {{ $ | goIDUpper }}Closable struct {
    {{$ | goIDUpper}}
}

func (c {{ $ | goIDUpper }}Closable) Close() error {
    var err error
    if v, ok := any(c.producer).({{goPkgExt "io"}}Closer); ok {
        err = {{goPkgExt "errors"}}Join(err, v.Close())
    }
    if v, ok := any(c.consumer).({{goPkgExt "io"}}Closer); ok {
        err = {{goPkgExt "errors"}}Join(err, v.Close())
    }
    return err
}

{{- if and $.IsPublisher $.IsSubscriber}}
    {{- with tryTmpl (print "code/proto/" $.Protocol "/server/impl/" .Name "/connectFunction") $}}
        {{.}}
    {{- else}}
        func Connect{{ $ | goIDUpper }}Bidi(
            ctx {{goPkgExt "context"}}Context,
            url *{{goPkgExt "net/url"}}URL,
            {{with $.SecuritySchemes}}security {{$ | goIDUpper}}Security,{{end}}
        ) (*{{ $ | goIDUpper }}Closable, error) {
            var bindings *{{goPkgUtil $.Protocol}}ServerBindings
            {{- if $.BindingsProtocols | has $.Protocol}}
                bindings = {{goPkgRun}}ToPtr({{goPkg $}}{{goIDUpper $}}ServerBindings{}.{{$.Protocol | goIDUpper}}())
            {{- end}}
            client, err := {{goPkgImpl .Protocol}}NewClient(ctx, url, bindings, {{if $.SecuritySchemes}}security{{else}}nil{{end}})
            if err != nil {
                return nil, err
            }
            producer, consumer := client, client
            return &{{ $ | goIDUpper }}Closable{
                {{$ | goIDUpper}}{producer: producer, consumer: consumer},
            }, nil
        }
    {{- end}}
{{- end}}

{{- if $.IsPublisher}}
    {{- with tryTmpl (print "code/proto/" $.Protocol "/server/impl/" .Name "/connectProducerFunction") $}}
        {{.}}
    {{- else}}
        func Connect{{ $ | goIDUpper }}Producer(
            ctx {{goPkgExt "context"}}Context,
            url *{{goPkgExt "net/url"}}URL,
            {{with $.SecuritySchemes}}security {{$ | goIDUpper}}Security,{{end}}
        ) (*{{ $ | goIDUpper }}Closable, error) {
            var bindings *{{goPkgUtil $.Protocol}}ServerBindings
            {{- if $.BindingsProtocols | has $.Protocol}}
                bindings = {{goPkgRun}}ToPtr({{goPkg $}}{{goIDUpper $}}ServerBindings{}.{{$.Protocol | goIDUpper}}())
            {{- end}}
            producer, err := {{goPkgImpl .Protocol}}NewClient(ctx, url, bindings, {{if $.SecuritySchemes}}security{{else}}nil{{end}})
            if err != nil {
                return nil, err
            }
            return &{{ $ | goIDUpper }}Closable{
                {{$ | goIDUpper}}{producer: producer},
            }, nil
        }
    {{- end}}
{{- end}}

{{- if $.IsSubscriber}}
    {{- with tryTmpl (print "code/proto/" $.Protocol "/server/impl/" .Name "/connectConsumerFunction") $}}
        {{.}}
    {{- else}}
        func Connect{{ $ | goIDUpper }}Consumer(
            ctx {{goPkgExt "context"}}Context,
            url *{{goPkgExt "net/url"}}URL,
            {{with $.SecuritySchemes}}security {{$ | goIDUpper}}Security,{{end}}
        ) (*{{ $ | goIDUpper }}Closable, error) {
            var bindings *{{goPkgUtil $.Protocol}}ServerBindings
            {{- if $.BindingsProtocols | has $.Protocol}}
                bindings = {{goPkgRun}}ToPtr({{goPkg $}}{{goIDUpper $}}ServerBindings{}.{{$.Protocol | goIDUpper}}())
            {{- end}}
            consumer, err := {{goPkgImpl .Protocol}}NewClient(ctx, url, bindings, {{if $.SecuritySchemes}}security{{else}}nil{{end}})
            if err != nil {
                return nil, err
            }
            return &{{ $ | goIDUpper }}Closable{
                {{$ | goIDUpper}}{consumer: consumer},
            }, nil
        }
    {{- end}}
{{- end}}
{{- end}}
{{- end}}

{{define "code/proto/server/channelOpenMethods"}}
{{- range .BoundChannels}}
    {{- if .ActiveProtocols | has $.Protocol }}
        func (s {{$ | goIDUpper}}) Open{{ . | goIDUpper }}{{$.Protocol | goIDUpper}}(
            ctx {{goPkgExt "context"}}Context,
            {{if .Parameters.Len}}params {{goPkg .}}{{goIDUpper .}}Parameters,{{end}}
            {{if .BoundOperations}}security {{goPkgRun}}AnySecurityScheme,{{end}}
        ) (*{{goPkg .}}{{ . | goIDUpper }}{{$.Protocol | goIDUpper}}, error) {
            return {{goPkg .}}Open{{ . | goIDUpper }}{{$.Protocol | goIDUpper}}(
                ctx, s, {{if .Parameters.Len}}params,{{end}}{{if .BoundOperations}}nil, security,{{end}}
            )
        }
    {{- end}}
{{- end}}
{{- end}}

{{define "code/proto/server/operationOpenMethods"}}
{{- range .BoundOperations}}
    {{- if .ActiveProtocols | has $.Protocol }}
        func (s {{$ | goIDUpper}}) Open{{ . | goIDUpper }}{{$.Protocol | goIDUpper}}(
            ctx {{goPkgExt "context"}}Context,
            {{if .Channel.Parameters.Len}}params {{goPkg .Channel}}{{goIDUpper .Channel}}Parameters,{{end}}
            {{if .SecuritySchemes}}security {{goPkg .}}{{. | goIDUpper}}Security,{{end}}
        ) (*{{goPkg .}}{{ . | goIDUpper }}{{$.Protocol | goIDUpper}}, error) {
            return {{goPkg .}}Open{{ . | goIDUpper }}{{$.Protocol | goIDUpper}}(
                ctx, s, {{if .Channel.Parameters.Len}}params, {{end}}{{if .SecuritySchemes}}security, {{end}}
            )
        }
    {{- end}}
{{- end}}
{{- end}}

{{define "code/proto/server/commonMethods" }}
func (s {{. | goIDUpper}}) Name() string {
    return "{{ . | goIDUpper }}"
}

func (s {{. | goIDUpper}}) Producer() {{goPkgUtil .Protocol}}Producer {
    return s.producer
}

func (s {{. | goIDUpper}}) Consumer() {{goPkgUtil .Protocol}}Consumer {
    return s.consumer
}

{{- if .BindingsProtocols | has .Protocol}}
func (s {{. | goIDUpper}}) Bindings() {{goPkgUtil .Protocol}}ServerBindings {
    return {{goPkg $}}{{goIDUpper $}}ServerBindings{}.{{.Protocol | goIDUpper}}()
}
{{- end}}

{{- end}}

{{template "code/proto/server/newFunction" .}}
{{- if .SecuritySchemes}}
    {{ template "code/proto/server/securityInterface" . }}
{{- end}}
{{- if impl .Protocol}}
    {{template "code/proto/server/connectFunctions" .}}
{{- end}}

{{- /* TODO: consider IsPublisher and IsSubscriber}} */}}
type {{. | goIDUpper}} struct {
    producer {{goPkgUtil .Protocol}}Producer
    consumer {{goPkgUtil .Protocol}}Consumer
}

{{template "code/proto/server/commonMethods" .}}
{{template "code/proto/server/channelOpenMethods" .}}
{{template "code/proto/server/operationOpenMethods" .}}
