{{- /* dot == render.Server */}}

{{define "code/proto/udp/server/impl/net/connectFunction"}}
func Connect{{ . | goIDUpper }}Bidi(
    ctx {{goPkgExt "context"}}Context,
    url *{{goPkgExt "net/url"}}URL,
    localAddress string,
    {{with $.SecuritySchemes}}security {{$ | goIDUpper}}Security,{{end}}
) (*{{ . | goIDUpper }}Closable, error) {
    client := {{goPkgImpl .Protocol}}NewClient(localAddress, url.Host, url.Scheme, {{if $.SecuritySchemes}}security{{else}}nil{{end}})
    consumer, producer := client, client
    return &{{ . | goIDUpper }}Closable{
        {{. | goIDUpper}}{producer: producer, consumer: consumer},
    }, nil
}
{{- end}}

{{define "code/proto/udp/server/impl/net/connectProducerFunction"}}
func Connect{{ . | goIDUpper }}Producer(
    ctx {{goPkgExt "context"}}Context,
    url *{{goPkgExt "net/url"}}URL,
    {{with $.SecuritySchemes}}security {{$ | goIDUpper}}Security,{{end}}
) (*{{ . | goIDUpper }}Closable, error) {
    producer := {{goPkgImpl .Protocol}}NewClient("", url.Host, url.Scheme, {{if $.SecuritySchemes}}security{{else}}nil{{end}})
    return &{{ . | goIDUpper }}Closable{
        {{. | goIDUpper}}{producer: producer},
    }, nil
}
{{- end}}

{{define "code/proto/udp/server/impl/net/connectConsumerFunction"}}
func Connect{{ . | goIDUpper }}Consumer(
    ctx {{goPkgExt "context"}}Context,
    url *{{goPkgExt "net/url"}}URL,
    {{with $.SecuritySchemes}}security {{$ | goIDUpper}}Security,{{end}}
) (*{{ . | goIDUpper }}Closable, error) {
    consumer := {{goPkgImpl .Protocol}}NewClient(url.Host, "", url.Scheme, {{if $.SecuritySchemes}}security{{else}}nil{{end}})
    return &{{ . | goIDUpper }}Closable{
        {{. | goIDUpper}}{consumer: consumer},
    }, nil
}
{{- end}}
