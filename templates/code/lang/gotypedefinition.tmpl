{{- /* dot == lang.GoTypeDefinition */}}

{{define "code/lang/gotypedefinition/definition"}}
    {{- if .Description }}
        {{ print (goID .) "--" .Description | goComment }}
    {{- end }}
    type {{ . | goID }} {{ .WrappedType | goDef }}
{{- end}}
{{define "code/lang/gotypedefinition/usage"}}
    {{- if .HasDefinition }}
        {{- if .Import }}
            {{- goPkgExt .Import }}{{goID .}}
        {{- else }}
            {{- goPkg . }}{{ . | goID }}
        {{- end }}
    {{- else }}
        {{- .WrappedType | goUsage }}
    {{- end }}
{{- end}}