{{define "definition"}}
    {{ .Name | goid }}
{{end}}
{{define "usage"}}
    {{- if and .Import (ne .Import .RenderContext.CurrentPackage) -}}
        {{ qual (print .Import "." .Name) }}
    {{- else -}}
        {{ .Name | goid }}
    {{- end -}}
{{end}}