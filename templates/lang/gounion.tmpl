{{define "lang/gounion/definition"}}
{{ .UnionStruct | godef }}

func (u *{{ . | goid }}) UnmarshalJSON(data []byte) (err error) {
{{- range .Fields}}
    if err = {{qual "encoding/json.Unmarshal"}}(bytes, {{if and (not .Type.IsPointer) .Type.Addressable}}&{{end}}u{{.Type.TypeName}}; err == nil {
        return
    }
{{- end}}
    return
}
{{- end}}
{{define "lang/gounion/usage"}}
    {{- template "lang/gostruct/usage" .}}
{{- end}}